# Vite environment variables (used in src/App.jsx)
# Copy this file to `.env` or `.env.local` and fill in the values.

# JSON string of your Firebase web config, e.g.:
# {
#   "apiKey": "...",
#   "authDomain": "...",
#   "projectId": "...",
#   "storageBucket": "...",
#   "messagingSenderId": "...",
#   "appId": "..."
# }
VITE_FIREBASE_CONFIG=

# Your application ID used for Firestore document paths
VITE_APP_ID=your-app-id

# Optional: custom auth token for Firebase Auth. Leave blank to use anonymous sign-in.
VITE_INITIAL_AUTH_TOKEN=
